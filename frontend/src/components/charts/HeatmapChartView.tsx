import React from 'react';

export const HeatmapChartView: React.FC = () => {
  return (
    <div
      style={{
        padding: '2rem',
        border: '1px dashed #aaa',
        borderRadius: '12px',
        textAlign: 'center',
        color: '#777',
      }}
    >
      <p>🔥 Heatmap view coming soon...</p>
      <p>(Like GitHub’s contribution calendar style)</p>
    </div>
  );
};
